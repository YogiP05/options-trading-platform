# `docs/` — Design docs, ADRs & runbooks

Long-form engineering documentation: architecture decision records (ADRs),
design notes, and operational runbooks.

The two top-level planning documents at the repo root are the north star:

- `../options-platform-plan.md` — architecture & strategy blueprint (the
  **what** and **why**), including §5 the tech stack.
- `../build-stage-plan.md` — the staged build plan (the **how** and the exit
  gates); S0-T1 implements the "Repo & build" workstream of **Stage S0**.

## Status (S0-T1)

Placeholder. As stages land, ADRs (e.g. "uv over Poetry", "devcontainer for the
dev env") and runbooks are added here.
