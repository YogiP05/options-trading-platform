.worktrees/
.polly/

# Rust
/target
**/target

# Python
__pycache__/
*.pyc
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
dist/
*.egg-info/

# Local secrets & config overrides (S0-T5) — see config/README.md.
# The committed samples are `.env.example` and `config/config.example.toml`.
.env
config/*.override.toml
